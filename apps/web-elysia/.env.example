# URL of the backend API worker this frontend proxies to.
# The web app is a pure proxy client: it never runs Better Auth or touches
# the database, so no auth secret or DB URL is needed here.
VITE_SERVER_URL=
