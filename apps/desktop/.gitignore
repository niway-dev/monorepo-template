node_modules
dist
out
.DS_Store
*.log*

# electron-builder's buildResources (icons + entitlements) are SOURCE inputs, not
# build output. The root .gitignore "build" rule would drop them, so re-include.
!build/
!build/**
