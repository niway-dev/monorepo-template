<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron's V8 needs JIT. This is the only entitlement every Electron app
       requires; add others only when a feature actually needs them, because each
       one widens what the notarised binary is allowed to do. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>

  <!-- Add ONLY if the app crashes on launch or hits library-validation errors:
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key><true/>
  <key>com.apple.security.cs.disable-library-validation</key><true/>
  -->

  <!-- Add per feature, e.g. a recorder:
  <key>com.apple.security.device.audio-input</key><true/>
  <key>com.apple.security.device.camera</key><true/>
  -->
</dict>
</plist>
