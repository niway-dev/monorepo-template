# Filled by `convex dev` for this deployment. SITE_URL is the .convex.site origin
# Better Auth serves its /api/auth/* routes on (set it in the Convex dashboard env
# or via `npx convex env set SITE_URL <your-deployment>.convex.site`).
CONVEX_DEPLOYMENT=
CONVEX_URL=
SITE_URL=
