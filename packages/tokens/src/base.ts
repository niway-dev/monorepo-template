import type { BaseTokens } from "./types";

/** Theme-invariant tokens — identical in light and dark. */
export const base: BaseTokens = {
  radius: "0.625rem",
  "font-sans": '"Inter Variable", sans-serif',
};
